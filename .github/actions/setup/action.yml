name: Setup
description: Setup the environment

inputs:
  node-version:
    description: The version of node.js
    required: false
    default: '18'

runs:
  using: composite
  steps:
    - name: Setup node
      uses: actions/setup-node@v3
      with:
        node-version: ${{ inputs.node-version }}
        cache: pnpm
        registry-url: 'https://registry.npmjs.org'

    - name: Install
      run: |
        corepack enable
        pnpm --version
        pnpm install
      shell: bash

    - name: Setup ni
      run: pnpm install --prefer-offline -g @antfu/ni
      shell: bash
