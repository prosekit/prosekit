/**
 * This file is auto-generated by the "@aria-ui/cli" tool. Do not edit this file directly.
 */

import {
  registerTableHandleColumnTriggerElement,
  type TableHandleColumnTriggerElement,
  type TableHandleColumnTriggerProps as TableHandleColumnTriggerElementProps,
} from "@prosekit/web/table-handle";
import {
  createElement,
  forwardRef,
  useCallback,
  useRef,
  type ForwardedRef,
  type ForwardRefExoticComponent,
  type HTMLAttributes,
  type RefAttributes,
  useLayoutEffect,
} from "react";

import { useEditorContext } from "../../contexts/editor-context.ts";

/**
 * Props for the {@link TableHandleColumnTrigger} React component.
 *
 * @public
 */
export interface TableHandleColumnTriggerProps extends HTMLAttributes<TableHandleColumnTriggerElement> {
  /**
   * @default null
   * @hidden
   */
  editor?: TableHandleColumnTriggerElementProps["editor"];
}

function TableHandleColumnTriggerComponent(
  props: TableHandleColumnTriggerProps,
  forwardedRef: ForwardedRef<TableHandleColumnTriggerElement>,
) {
  registerTableHandleColumnTriggerElement();

  const elementRef = useRef<TableHandleColumnTriggerElement>(null);

  const p0Fallback = useEditorContext();

  const { editor: p0, ...restProps } = props;

  useLayoutEffect(() => {
    const element = elementRef.current as Record<string, unknown> | null;
    if (!element) return;
    Object.assign(element, { editor: p0 ?? p0Fallback });
  });

  const mergedRef = useCallback(
    (element: TableHandleColumnTriggerElement | null) => {
      elementRef.current = element;
      if (typeof forwardedRef === "function") {
        forwardedRef(element);
      } else if (forwardedRef) {
        forwardedRef.current = element;
      }
    },
    [],
  );

  return createElement("prosekit-table-handle-column-trigger", {
    ...restProps,
    ref: mergedRef,
    suppressHydrationWarning: true,
  });
}

/**
 * A React component that renders an `prosekit-table-handle-column-trigger` custom element.
 *
 * @public
 */
export const TableHandleColumnTrigger: ForwardRefExoticComponent<
  TableHandleColumnTriggerProps & RefAttributes<TableHandleColumnTriggerElement>
> = /* @__PURE__ */ forwardRef(TableHandleColumnTriggerComponent);
