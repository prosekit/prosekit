---
"@prosekit/extensions": minor
"prosekit": minor
---

Add a `followCursor` option to `AutocompleteRule` that keeps the match end anchored to the text cursor, so cursor movement grows and shrinks the query over existing text instead of dismissing the match.
