// This file is auto-generated by "@aria-ui/cli". Do not edit this file directly.

import { registerColumnsPopoverRootElement, type ColumnsPopoverRootElement, type ColumnsPopoverRootEvents, type ColumnsPopoverRootProps as ColumnsPopoverRootElementProps } from '@prosekit/web/columns-popover';
import { createEffect, createSignal, mergeProps, splitProps } from 'solid-js';
import type { Component, JSX } from 'solid-js';
import h from 'solid-js/h';

import { useEditorContext } from '../../contexts/editor-context.ts';

/** Props for the {@link ColumnsPopoverRoot} Solid component. */
export interface ColumnsPopoverRootProps {
  /**
   * The ProseKit editor instance.
   *
   * @default null
   * @hidden
   */
  editor?: ColumnsPopoverRootElementProps['editor'];
  /**
   * Whether the popover is open by default when the cursor is inside a column.
   *
   * @default true
   */
  defaultOpen?: ColumnsPopoverRootElementProps['defaultOpen'];
  /**
   * Whether the columns popover should be dismissed when the editor receives
   * an Escape key press.
   *
   * @default true
   */
  dismissOnEscape?: ColumnsPopoverRootElementProps['dismissOnEscape'];
  /**
   * Whether the overlay is currently open.
   * @default null
   */
  open?: ColumnsPopoverRootElementProps['open'];
  /**
   * Whether the component should ignore user interaction.
   * @default false
   */
  disabled?: ColumnsPopoverRootElementProps['disabled'];
  /** Emitted when the open state of the popover changes. */
  onOpenChange?: (event: ColumnsPopoverRootEvents['openChange']) => void;
}

/** A Solid component that renders an `prosekit-columns-popover-root` custom element. */
export const ColumnsPopoverRoot: Component<ColumnsPopoverRootProps & JSX.HTMLAttributes<ColumnsPopoverRootElement>> = (props): any => {
  registerColumnsPopoverRootElement();

  const [getElement, setElement] = createSignal<ColumnsPopoverRootElement | null>(null);
  const handlers: Array<((event: any) => void) | undefined> = [];

  const [elementProps, eventHandlers, restProps] = splitProps(props, ['defaultOpen', 'disabled', 'dismissOnEscape', 'editor', 'open'], ['onOpenChange']);

  const p3Fallback = useEditorContext();

  createEffect(() => {
    const element = getElement();
    if (!element) return;

    Object.assign(element, { defaultOpen: elementProps.defaultOpen, disabled: elementProps.disabled, dismissOnEscape: elementProps.dismissOnEscape, editor: elementProps.editor ?? p3Fallback, open: elementProps.open });

    handlers.length = 0;
    handlers.push(eventHandlers.onOpenChange);
  });

  createEffect(() => {
    const element = getElement();
    if (!element) return;

    const ac = new AbortController();
    for (const [index, eventName] of ['openChange'].entries()) {
      element.addEventListener(
        eventName,
        (event) => {
          handlers[index]?.(event);
        },
        { signal: ac.signal },
      );
    }
    return () => ac.abort();
  });

  return () =>
    h(
      'prosekit-columns-popover-root',
      mergeProps(restProps, {
        ref: (el: ColumnsPopoverRootElement | null) => {
          setElement(el);
        },
      }),
    );
};
