/**
 * This file is auto-generated by the "@aria-ui/cli" tool. Do not edit this file directly.
 */

import {
  registerTableHandlePopoverPopupElement,
  type TableHandlePopoverPopupElement,
  type TableHandlePopoverPopupProps as TableHandlePopoverPopupElementProps,
} from "@prosekit/web/table-handle";
import { createEffect, createSignal, mergeProps, splitProps } from "solid-js";
import type { Component, JSX } from "solid-js";
import h from "solid-js/h";

/**
 * Props for the {@link TableHandlePopoverPopup} Solid component.
 *
 * @public
 */
export interface TableHandlePopoverPopupProps extends JSX.HTMLAttributes<TableHandlePopoverPopupElement> {
  /**
   * By default, the popup element will listen for keydown events.
   * You can pass a different element to listen for keydown events.
   *
   * @default null
   */
  eventTarget?: TableHandlePopoverPopupElementProps["eventTarget"];
}

/**
 * A Solid component that renders an `prosekit-table-handle-popover-popup` custom element.
 *
 * @public
 */
export const TableHandlePopoverPopup: Component<
  TableHandlePopoverPopupProps
> = (props): any => {
  registerTableHandlePopoverPopupElement();

  const [getElement, setElement] =
    createSignal<TableHandlePopoverPopupElement | null>(null);

  const [elementProps, restProps] = splitProps(props, ["eventTarget"]);

  createEffect(() => {
    const element = getElement();
    if (!element) return;

    Object.assign(element, {
      eventTarget: elementProps.eventTarget,
    });
  });

  return h(
    "prosekit-table-handle-popover-popup",
    mergeProps(restProps, {
      ref: (el: TableHandlePopoverPopupElement | null) => {
        setElement(el);
      },
    }),
  );
};
