/**
 * This file is auto-generated by the "@aria-ui/cli" tool. Do not edit this file directly.
 */

import {
  registerTableHandleColumnTriggerElement,
  type TableHandleColumnTriggerElement,
  type TableHandleColumnTriggerProps as TableHandleColumnTriggerElementProps,
} from "@prosekit/web/table-handle";
import { createEffect, createSignal, mergeProps, splitProps } from "solid-js";
import type { Component, JSX } from "solid-js";
import h from "solid-js/h";
import { useEditorContext } from "../../contexts/editor-context.ts";

/**
 * Props for the {@link TableHandleColumnTrigger} Solid component.
 *
 * @public
 */
export interface TableHandleColumnTriggerProps extends JSX.HTMLAttributes<TableHandleColumnTriggerElement> {
  /**
   * @default null
   * @hidden
   */
  editor?: TableHandleColumnTriggerElementProps["editor"];
}

/**
 * A Solid component that renders an `prosekit-table-handle-column-trigger` custom element.
 *
 * @public
 */
export const TableHandleColumnTrigger: Component<
  TableHandleColumnTriggerProps
> = (props): any => {
  registerTableHandleColumnTriggerElement();

  const [getElement, setElement] =
    createSignal<TableHandleColumnTriggerElement | null>(null);

  const [elementProps, restProps] = splitProps(props, ["editor"]);

  const p0Fallback = useEditorContext();

  createEffect(() => {
    const element = getElement();
    if (!element) return;

    Object.assign(element, {
      editor: elementProps.editor ?? p0Fallback,
    });
  });

  return h(
    "prosekit-table-handle-column-trigger",
    mergeProps(restProps, {
      ref: (el: TableHandleColumnTriggerElement | null) => {
        setElement(el);
      },
    }),
  );
};
