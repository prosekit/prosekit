---
title: Link
---

import Demo from '@/components/ui/demo/demo.astro';

The `link` mark is used to represent links. It will be rendered as `<a>` element in HTML.

<Demo story="link" />

## Commands

### `addLink`

```ts twoslash
import { createEditor } from 'prosekit/core'
import { defineLink } from 'prosekit/extensions/link'

const extension = defineLink()
const editor = createEditor({ extension })
// ---cut---
editor.commands.addLink({ href: 'https://www.example.com' })
```

### `removeLink`

```ts twoslash
import { createEditor } from 'prosekit/core'
import { defineLink } from 'prosekit/extensions/link'

const extension = defineLink()
const editor = createEditor({ extension })
// ---cut---
editor.commands.removeLink()
```

### `toggleLink`

```ts twoslash
import { createEditor } from 'prosekit/core'
import { defineLink } from 'prosekit/extensions/link'

const extension = defineLink()
const editor = createEditor({ extension })
// ---cut---
editor.commands.toggleLink({ href: 'https://www.example.com' })
```

## API Reference

- [prosekit/extensions/link](/references/extensions/link)
