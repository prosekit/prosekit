/**
 * This file is auto-generated by the "@aria-ui/cli" tool. Do not edit this file directly.
 */

import {
  registerAutocompletePopoverElement,
  type AutocompletePopoverElement,
  type AutocompletePopoverEvents,
  type AutocompletePopoverProps as AutocompletePopoverElementProps,
} from "@prosekit/web/autocomplete";
import { createEffect, createSignal, mergeProps, splitProps } from "solid-js";
import type { Component, JSX } from "solid-js";
import h from "solid-js/h";

import { useEditorContext } from "../../contexts/editor-context.ts";

/**
 * Props for the {@link AutocompletePopover} Solid component.
 *
 * @public
 */
export interface AutocompletePopoverProps extends JSX.HTMLAttributes<AutocompletePopoverElement> {
  /**
   * The ProseKit editor instance.
   *
   * @default null
   * @hidden
   */
  editor?: AutocompletePopoverElementProps["editor"];
  /**
   * The regular expression to match the query text to autocomplete.
   *
   * @default null
   */
  regex?: AutocompletePopoverElementProps["regex"];
  /**
   * The placement of the popover, relative to the text cursor.
   *
   * @default "bottom-start"
   */
  placement?: AutocompletePopoverElementProps["placement"];
  /**
   * The distance between the popover and the hovered block.
   *
   * @default 4
   */
  offset?: AutocompletePopoverElementProps["offset"];
  /** @default true */
  inline?: AutocompletePopoverElementProps["inline"];
  /** @default true */
  hoist?: AutocompletePopoverElementProps["hoist"];
  /** @default true */
  fitViewport?: AutocompletePopoverElementProps["fitViewport"];
  /** @default "The body element" */
  boundary?: AutocompletePopoverElementProps["boundary"];
  /** @default 8 */
  overflowPadding?: AutocompletePopoverElementProps["overflowPadding"];
  /**
   * The strategy to use for positioning
   *
   * @default "absolute"
   */
  strategy?: AutocompletePopoverElementProps["strategy"];
  /**
   * Options to activate auto-update listeners
   *
   * @see https://floating-ui.com/docs/autoUpdate
   *
   * @default true
   */
  autoUpdate?: AutocompletePopoverElementProps["autoUpdate"];
  /**
   * Whether to flip the `placement` in order to keep it in view when the
   * preferred placement(s) will overflow the clipping boundary. You can also
   * provide an array of placements to try sequentially if the preferred
   * `placement` does not fit.
   *
   * @default true
   */
  flip?: AutocompletePopoverElementProps["flip"];
  /**
   * Whether the floating element should shift to keep it in view.
   *
   * @default true
   */
  shift?: AutocompletePopoverElementProps["shift"];
  /**
   * Whether the floating element can overlap the reference element to keep it
   * in view.
   *
   * @default false
   */
  overlap?: AutocompletePopoverElementProps["overlap"];
  /**
   * Whether to constrain the floating element's width so that it matches the
   * reference element.
   *
   * @default false
   */
  sameWidth?: AutocompletePopoverElementProps["sameWidth"];
  /**
   * Whether to constrain the floating element's height so that it matches the
   * reference element.
   *
   * @default false
   */
  sameHeight?: AutocompletePopoverElementProps["sameHeight"];
  /**
   * Whether to hide the floating element when the reference element or the
   * floating element is fully clipped.
   *
   * @default false
   */
  hide?: AutocompletePopoverElementProps["hide"];
  /**
   * Describes the root boundary that the element will be checked for overflow relative to.
   * Please see https://floating-ui.com/docs/detectoverflow#rootboundary for more information.
   *
   * @default 'viewport'
   */
  rootBoundary?: AutocompletePopoverElementProps["rootBoundary"];
  /**
   * The element that will be used to check for overflow. Please see
   * https://floating-ui.com/docs/detectoverflow#elementcontext for more
   * information.
   *
   * @default 'floating'
   */
  elementContext?: AutocompletePopoverElementProps["elementContext"];
  /**
   * Whether to check the alternate elementContext's boundary. Please see
   * https://floating-ui.com/docs/detectoverflow#altboundary for more
   * information.
   *
   * @default false
   */
  altBoundary?: AutocompletePopoverElementProps["altBoundary"];
  /** Fired when the open state changes. */
  onOpenChange?: (event: AutocompletePopoverEvents["openChange"]) => void;
  /** Fired when the query changes. */
  onQueryChange?: (event: AutocompletePopoverEvents["queryChange"]) => void;
}

/**
 * A Solid component that renders an `prosekit-autocomplete-popover` custom element.
 *
 * @public
 */
export const AutocompletePopover: Component<AutocompletePopoverProps> = (
  props,
): any => {
  registerAutocompletePopoverElement();

  const [getElement, setElement] =
    createSignal<AutocompletePopoverElement | null>(null);
  const handlers: Array<((event: any) => void) | undefined> = [];

  const [elementProps, eventHandlers, restProps] = splitProps(
    props,
    [
      "altBoundary",
      "autoUpdate",
      "boundary",
      "editor",
      "elementContext",
      "fitViewport",
      "flip",
      "hide",
      "hoist",
      "inline",
      "offset",
      "overflowPadding",
      "overlap",
      "placement",
      "regex",
      "rootBoundary",
      "sameHeight",
      "sameWidth",
      "shift",
      "strategy",
    ],
    ["onOpenChange", "onQueryChange"],
  );

  const p3Fallback = useEditorContext();

  createEffect(() => {
    const element = getElement();
    if (!element) return;

    Object.assign(element, {
      altBoundary: elementProps.altBoundary,
      autoUpdate: elementProps.autoUpdate,
      boundary: elementProps.boundary,
      editor: elementProps.editor ?? p3Fallback,
      elementContext: elementProps.elementContext,
      fitViewport: elementProps.fitViewport,
      flip: elementProps.flip,
      hide: elementProps.hide,
      hoist: elementProps.hoist,
      inline: elementProps.inline,
      offset: elementProps.offset,
      overflowPadding: elementProps.overflowPadding,
      overlap: elementProps.overlap,
      placement: elementProps.placement,
      regex: elementProps.regex,
      rootBoundary: elementProps.rootBoundary,
      sameHeight: elementProps.sameHeight,
      sameWidth: elementProps.sameWidth,
      shift: elementProps.shift,
      strategy: elementProps.strategy,
    });

    handlers.length = 0;
    handlers.push(eventHandlers.onOpenChange);
    handlers.push(eventHandlers.onQueryChange);
  });

  createEffect(() => {
    const element = getElement();
    if (!element) return;

    const ac = new AbortController();
    for (const [index, eventName] of ["openChange", "queryChange"].entries()) {
      element.addEventListener(
        eventName,
        (event) => {
          handlers[index]?.(event);
        },
        { signal: ac.signal },
      );
    }
    return () => ac.abort();
  });

  return h(
    "prosekit-autocomplete-popover",
    mergeProps(restProps, {
      ref: (el: AutocompletePopoverElement | null) => {
        setElement(el);
      },
    }),
  );
};

export type { AutocompletePopoverEvents };
export {
  AutocompletePopoverOpenChangeEvent,
  AutocompletePopoverQueryChangeEvent,
} from "@prosekit/web/autocomplete";
