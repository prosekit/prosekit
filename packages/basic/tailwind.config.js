/** @type {import('tailwindcss').Config} */
const config = {
  darkMode: 'class',
  content: ['./components/**/*.{html,js,ts}'],
  theme: {
    extend: {},
  },
  plugins: [],
}

export default config
