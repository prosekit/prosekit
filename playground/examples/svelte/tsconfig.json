{
  "extends": "../../../tsconfig.json",
  "compilerOptions": {
    // Turn off composite to avoid the following error: The inferred type of
    // 'defineExtension' cannot be named without a reference to
    // 'prosemirror-flat-list'. This is likely not portable. A type annotation
    // is necessary.
    "composite": false,
    "outDir": ".temp",
  },
  "include": ["./**/*.ts", "./**/*.svelte"],
}
