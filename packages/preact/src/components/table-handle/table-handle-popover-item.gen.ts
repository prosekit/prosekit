/**
 * This file is auto-generated by the "@aria-ui/cli" tool. Do not edit this file directly.
 */

import { createElement, type HTMLAttributes, type Ref } from "preact";
import {
  forwardRef,
  type ForwardRefExoticComponent,
  type RefAttributes,
} from "preact/compat";
import { useCallback, useRef, useLayoutEffect } from "preact/hooks";
import {
  registerTableHandlePopoverItemElement,
  type TableHandlePopoverItemElement,
  type TableHandlePopoverItemProps as TableHandlePopoverItemElementProps,
  type TableHandlePopoverItemEvents,
} from "@prosekit/web/table-handle";

/**
 * Props for the {@link TableHandlePopoverItem} Preact component.
 *
 * @public
 */
export interface TableHandlePopoverItemProps extends HTMLAttributes<TableHandlePopoverItemElement> {
  /** @default "" */
  value?: TableHandlePopoverItemElementProps["value"];
  /** @default false */
  disabled?: TableHandlePopoverItemElementProps["disabled"];
  /** */
  onItemSelect?: (event: TableHandlePopoverItemEvents["itemSelect"]) => void;
}
function TableHandlePopoverItemComponent(
  props: TableHandlePopoverItemProps,
  forwardedRef: Ref<TableHandlePopoverItemElement>,
) {
  registerTableHandlePopoverItemElement();

  const elementRef = useRef<TableHandlePopoverItemElement>(null);
  const handlersRef = useRef<Array<((event: Event) => void) | undefined>>([]);

  const { disabled: p0, value: p1, onItemSelect: e0, ...restProps } = props;

  useLayoutEffect(() => {
    const element = elementRef.current as Record<string, unknown> | null;
    if (!element) return;
    Object.assign(element, { disabled: p0, value: p1 });
    handlersRef.current = [e0] as Array<((event: Event) => void) | undefined>;
  });

  useLayoutEffect(() => {
    const element = elementRef.current;
    if (!element) return;
    const ac = new AbortController();
    for (const [index, eventName] of ["itemSelect"].entries()) {
      element.addEventListener(
        eventName,
        (event: Event) => {
          handlersRef.current[index]?.(event);
        },
        { signal: ac.signal },
      );
    }
    return () => ac.abort();
  }, []);

  const mergedRef = useCallback(
    (element: TableHandlePopoverItemElement | null) => {
      elementRef.current = element;
      if (typeof forwardedRef === "function") {
        forwardedRef(element);
      } else if (forwardedRef) {
        forwardedRef.current = element;
      }
    },
    [],
  );

  return createElement("prosekit-table-handle-popover-item", {
    ...restProps,
    ref: mergedRef,
    suppressHydrationWarning: true,
  });
}

/**
 * A Preact component that renders an `prosekit-table-handle-popover-item` custom element.
 *
 * @public
 */
export const TableHandlePopoverItem: ForwardRefExoticComponent<
  TableHandlePopoverItemProps & RefAttributes<TableHandlePopoverItemElement>
> = /* @__PURE__ */ forwardRef(TableHandlePopoverItemComponent);

export type { TableHandlePopoverItemEvents };
export { TableHandlePopoverItemSelectEvent } from "@prosekit/web/table-handle";
