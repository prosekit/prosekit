---
import { ReactRenderer } from 'prosekit-registry/react/renderer'
import Container from './container.astro'
interface Props {
  story: string
}
---

<Container>
  <ReactRenderer story={Astro.props.story} client:only="react" />
</Container>
