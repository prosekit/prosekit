/**
 * This file is auto-generated by the "@aria-ui/cli" tool. Do not edit this file directly.
 */

import {
  registerAutocompleteListElement,
  type AutocompleteListElement,
  type AutocompleteListEvents,
  type AutocompleteListProps as AutocompleteListElementProps,
} from "@prosekit/web/autocomplete";
import { createEffect, createSignal, mergeProps, splitProps } from "solid-js";
import type { Component, JSX } from "solid-js";
import h from "solid-js/h";

import { useEditorContext } from "../../contexts/editor-context.ts";

/**
 * Props for the {@link AutocompleteList} Solid component.
 *
 * @public
 */
export interface AutocompleteListProps extends JSX.HTMLAttributes<AutocompleteListElement> {
  /**
   * The ProseKit editor instance.
   *
   * @default null
   * @hidden
   */
  editor?: AutocompleteListElementProps["editor"];
  /**
   * The filter function to determine if an item should be shown in the
   * listbox.
   *
   * @default defaultItemFilter
   */
  filter?: AutocompleteListElementProps["filter"];
  /**
   * Emitted when the selected value changes. Only available when multiple is
   * false.
   */
  onValueChange?: (event: AutocompleteListEvents["valueChange"]) => void;
  /**
   * Emitted when the selected values change. Only available when multiple is
   * true.
   */
  onValuesChange?: (event: AutocompleteListEvents["valuesChange"]) => void;
}

/**
 * A Solid component that renders an `prosekit-autocomplete-list` custom element.
 *
 * @public
 */
export const AutocompleteList: Component<AutocompleteListProps> = (
  props,
): any => {
  registerAutocompleteListElement();

  const [getElement, setElement] = createSignal<AutocompleteListElement | null>(
    null,
  );
  const handlers: Array<((event: any) => void) | undefined> = [];

  const [elementProps, eventHandlers, restProps] = splitProps(
    props,
    ["editor", "filter"],
    ["onValueChange", "onValuesChange"],
  );

  const p0Fallback = useEditorContext();

  createEffect(() => {
    const element = getElement();
    if (!element) return;

    Object.assign(element, {
      editor: elementProps.editor ?? p0Fallback,
      filter: elementProps.filter,
    });

    handlers.length = 0;
    handlers.push(eventHandlers.onValueChange);
    handlers.push(eventHandlers.onValuesChange);
  });

  createEffect(() => {
    const element = getElement();
    if (!element) return;

    const ac = new AbortController();
    for (const [index, eventName] of [
      "valueChange",
      "valuesChange",
    ].entries()) {
      element.addEventListener(
        eventName,
        (event) => {
          handlers[index]?.(event);
        },
        { signal: ac.signal },
      );
    }
    return () => ac.abort();
  });

  return h(
    "prosekit-autocomplete-list",
    mergeProps(restProps, {
      ref: (el: AutocompleteListElement | null) => {
        setElement(el);
      },
    }),
  );
};

export type { AutocompleteListEvents };
export {
  ValueChangeEvent,
  ValuesChangeEvent,
} from "@prosekit/web/autocomplete";
