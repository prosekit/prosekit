---
import { VueRenderer } from 'prosekit-registry/vue/renderer'
import Container from './container.astro'

interface Props {
  story: string
}
---

<Container>
  <VueRenderer story={Astro.props.story} client:only="vue" />
</Container>
