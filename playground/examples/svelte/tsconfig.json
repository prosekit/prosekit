{
  "extends": "../../../tsconfig.json",
  "compilerOptions": {
    "composite": true,

    // `declarationMap` doesn't work well with `svelte-check`
    "declarationMap": false,
  },
  "include": ["./**/*.ts", "./**/*.svelte"],
  "references": [
    {
      "path": "../../components/svelte",
    },
  ],
}
