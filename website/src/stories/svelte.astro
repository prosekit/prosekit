---
import { SvelteRenderer } from 'prosekit-registry/svelte/renderer'
import Container from './container.astro'

interface Props {
  story: string
}
---

<Container>
  <SvelteRenderer story={Astro.props.story} client:only="svelte" />
</Container>
