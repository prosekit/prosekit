/**
 * This file is auto-generated by the "@aria-ui/cli" tool. Do not edit this file directly.
 */

import {
  registerTableHandlePopoverItemElement,
  type TableHandlePopoverItemElement,
  type TableHandlePopoverItemEvents,
  type TableHandlePopoverItemProps as TableHandlePopoverItemElementProps,
} from "@prosekit/web/table-handle";
import { createEffect, createSignal, mergeProps, splitProps } from "solid-js";
import type { Component, JSX } from "solid-js";
import h from "solid-js/h";

/**
 * Props for the {@link TableHandlePopoverItem} Solid component.
 *
 * @public
 */
export interface TableHandlePopoverItemProps extends JSX.HTMLAttributes<TableHandlePopoverItemElement> {
  /** @default "" */
  value?: TableHandlePopoverItemElementProps["value"];
  /** @default false */
  disabled?: TableHandlePopoverItemElementProps["disabled"];
  /** */
  onItemSelect?: (event: TableHandlePopoverItemEvents["itemSelect"]) => void;
}

/**
 * A Solid component that renders an `prosekit-table-handle-popover-item` custom element.
 *
 * @public
 */
export const TableHandlePopoverItem: Component<TableHandlePopoverItemProps> = (
  props,
): any => {
  registerTableHandlePopoverItemElement();

  const [getElement, setElement] =
    createSignal<TableHandlePopoverItemElement | null>(null);
  const handlers: Array<((event: any) => void) | undefined> = [];

  const [elementProps, eventHandlers, restProps] = splitProps(
    props,
    ["disabled", "value"],
    ["onItemSelect"],
  );

  createEffect(() => {
    const element = getElement();
    if (!element) return;

    Object.assign(element, {
      disabled: elementProps.disabled,
      value: elementProps.value,
    });

    handlers.length = 0;
    handlers.push(eventHandlers.onItemSelect);
  });

  createEffect(() => {
    const element = getElement();
    if (!element) return;

    const ac = new AbortController();
    for (const [index, eventName] of ["itemSelect"].entries()) {
      element.addEventListener(
        eventName,
        (event) => {
          handlers[index]?.(event);
        },
        { signal: ac.signal },
      );
    }
    return () => ac.abort();
  });

  return h(
    "prosekit-table-handle-popover-item",
    mergeProps(restProps, {
      ref: (el: TableHandlePopoverItemElement | null) => {
        setElement(el);
      },
    }),
  );
};

export type { TableHandlePopoverItemEvents };
export { TableHandlePopoverItemSelectEvent } from "@prosekit/web/table-handle";
