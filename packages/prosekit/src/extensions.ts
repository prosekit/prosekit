/**
 * `prosekit/extensions` is a module that contains extensions. You can find multiple sub-modules for different extensions.
 *
 * @module prosekit/extensions
 */

export * from '@prosekit/extensions'
