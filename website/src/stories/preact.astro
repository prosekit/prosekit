---
import { PreactRenderer } from 'prosekit-registry/preact/renderer'
import Container from './container.astro'

interface Props {
  story: string
}
---

<Container>
  <PreactRenderer story={Astro.props.story} client:only="preact" />
</Container>
